
<view class="main">
  <view class="left">
    <scroll-view scroll-y='true' >
      <view wx:for-item="itemx" wx:for="{{navLeftData}}" wx:key="index" class="left-son" bindtap="changeTab" data-name="{{itemx}}">{{itemx}}</view>
    </scroll-view>
  </view>
  <view class="right">
    <scroll-view scroll-y="true" >
      <view class="right-c">
        <view class="right-item" wx:for="{{classIfyList}}" wx:key="index">
          <view class="right-item-img"><image style="width: 100%;height: 100%;" src="{{item.url}}"></image></view>
          <view class="right-tiem-text">{{item.text}}</view>
       </view>
      </view>
    </scroll-view>
  </view>
</view>
